{
  "extends": "next/core-web-vitals",
  "overrides": [
    {
      "files": ["**/*.test.tsx", "**/*.test.ts"],
      "rules": {
        "react/display-name": "off" // Disable display-name rule for test files
      }
    }
  ]
}
